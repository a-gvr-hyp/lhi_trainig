<mvc:View
   controllerName="motocyclefreestyle.controller.InvoiceList"
   xmlns="sap.m"
   xmlns:core="sap.ui.core"
   xmlns:mvc="sap.ui.core.mvc">
   <List
      class="sapUiResponsiveMargin"
      id="invoiceList"
      width="auto"
      items="{
         path : 'motocycleModel>/Motocycle'
      }">
      <headerToolbar>
         <Toolbar>
            <Title text="{i18n>invoiceListTitle}"/>
            <ToolbarSpacer/>
            <Button
               text="{i18n>addMotocycleButtonText}"
               press=".onAddMotocycle"/>
            <SearchField 
               width="50%" 
               search=".onFilterInvoices"/>
         </Toolbar>
      </headerToolbar>
      <items>
         <ObjectListItem
            core:require="{
                  Currency: 'sap/ui/model/type/Currency'
               }"
            title="{motocycleModel>name} - {motocycleModel>farbe}"
            number="{
                  parts: [
                     'motocycleModel>discountPrice',
                     'view>/currency'
                 ],
                 type: 'Currency',
                 formatOptions: {
                     showMeasure: false
               }
            }"
            numberUnit="{view>/currency}"
            numberState="{= ${motocycleModel>hasDiscount} ? 'Success' : 'Information' }"
            type="Navigation"
            press=".onPress">
               <firstStatus>
                    <ObjectStatus
                        text="{
                            path: 'motocycleModel>hasDiscount',
                            formatter: '.statusText'
                        }"/>
                </firstStatus>
                <ObjectAttribute text="Hubraum: {motocycleModel>hubraum}" />
                <ObjectAttribute text="Farbe: {motocycleModel>farbe}" />
                <ObjectAttribute text="Price: {motocycleModel>preis}" />
            </ObjectListItem>
      </items>
   </List>
</mvc:View>